# TradingAgents-Astock 分析师工具全景分析

> 版本: v0.4.0 | 日期: 2026-08-04

---

## 一、数据来源概览

本项目所有工具的数据获取有三种方式，按"是否依赖浏览器"分为两类：

| 类别 | 说明 | 依赖浏览器? |
|---|---|---|
| **直接 HTTP API** | 直连腾讯/东财/同花顺/新浪/财联社 HTTP 接口，零浏览器 | ❌ 否 |
| **Playwright 服务** | 通过独立 `playwright_service` 服务的 Chrome CDP 抓取同花顺F10/问财/东财页面 | ✅ **是** |
| **纯算法** | 本地计算（K线形态识别、筹码分布 CYQ 算法），数据来源于上面两类 | 视输入来源而定 |

> **架构要点**：playwright 工具并不在主进程中运行 playwright，而是通过 `PlaywrightClient` HTTP 调用一个独立的外部服务（`playwright_service/server.py`，18 个数据端点 + `/api/health` + `/api/routes`），该服务在单独的 conda 环境 `worktrade2` 中运行 Chrome CDP。但对调用者而言逻辑上依赖浏览器。
>
> 熔断器保护：5 次连续失败后熔断 60 秒，`threading.Lock` 线程安全（`AKS_CIRCUIT_THRESHOLD` / `AKS_CIRCUIT_COOLDOWN` 可调）。

---

## 二、全部工具清单（33 个唯一个体函数）

### 2.1 直连 HTTP API 工具（16 个，零浏览器依赖）

| 工具函数 | 底层数据源 | HTTP 端点 |
|---|---|---|
| `get_fundamentals` | 腾讯行情API + mootdx(TCP) + 东财HTTP | `qt.gtimg.cn` / mootdx:7709 / 东财 |
| `get_balance_sheet` | 新浪财经 HTTP | `money.finance.sina.com.cn` |
| `get_cashflow` | 新浪财经 HTTP | `money.finance.sina.com.cn` |
| `get_income_statement` | 新浪财经 HTTP | `money.finance.sina.com.cn` |
| `get_news` | 东方财富 HTTP + 新浪财经(fallback) | `search-api-web.eastmoney.com`(搜索API) / 新浪 |
| `get_global_news` | 财联社 HTTP | `cls.cn` |
| `get_hot_stocks` | 同花顺 HTTP | 同花顺 hot stock API |
| `get_northbound_flow` | 同花顺 hsgtApi HTTP + 本地CSV缓存 | `data.hexin.cn`（2024-08 起上游停更历史，历史收盘由本地缓存累计） |
| `get_dragon_tiger_board` | 东方财富 datacenter HTTP | `datacenter-web.eastmoney.com` |
| `get_lockup_expiry` | 东方财富 datacenter HTTP | `datacenter-web.eastmoney.com` |
| `get_industry_comparison` | 东方财富 datacenter HTTP | `datacenter-web.eastmoney.com` |
| `get_chip_distribution` | 东财 push2his HTTP + Python CYQ算法 | `push2his.eastmoney.com` |
| `get_limit_up_pool` | 东方财富 push2ex HTTP | `push2ex.eastmoney.com` |
| `get_indicators` | mootdx TCP + 技术指标计算 | mootdx:7709 |
| `get_stock_data` | mootdx TCP | mootdx:7709 |
| `get_profit_forecast` (a_stock版) | 同花顺 HTTP 直连 | 同花顺 EPS 预测直连 |

### 2.2 Playwright 浏览器工具（15 个，依赖浏览器）

| 工具函数 | 数据源 | 同花顺F10页面/说明 |
|---|---|---|
| `get_stock_basic` | 同花顺F10 | 股本结构 |
| `get_stock_homepage` | 同花顺F10 | 首页综合 |
| `get_stock_industry_peers` | 同花顺F10 | 行业对标 |
| `get_stock_holder` | 同花顺F10 | 股东研究 |
| `get_stock_equity_history` | 同花顺F10 | 股本历史 |
| `get_stock_position` | 同花顺F10 | 主力持仓 |
| `get_financial_quarterly` | 同花顺F10 | finance.html (季频财务) |
| `get_profit_forecast_playwright` | 同花顺F10 | EPS 预测（enrichment 版本） |
| `get_insider_transactions` | 同花顺F10 | 公司大事（event.html，经 playwright_service `/api/company-events`；a_stock vendor 内部实现走该服务） |
| `get_market_context` | 东财行情（playwright） | 大盘概览 |
| `get_stock_kline_full` | 东财 push2his（playwright） | 增强K线 |
| `get_stock_levels` | 同花顺问财 | 支撑压力位 |
| `get_industry_hotmap` | 东财星图（playwright） | 行业热力（行业涨跌/主力净流入热力图） |
| `get_concept_blocks_playwright` | 同花顺问财 | 概念归属（enrichment 版本） |
| `get_fund_flow_playwright` | 同花顺问财 | 资金流（enrichment 版本） |

### 2.3 组合工具（2 个，间接依赖浏览器）

| 工具函数 | 实现方式 | 数据来源链 |
|---|---|---|
| `analyze_pattern` | 纯算法（analysis_tools.py） | 内部调用 playwright 的 `get_stock_kline_full` 获取K线数据 → 本地12种形态检测 |
| `verify_stock_valuation` | Decimal精确计算（financial_rigor.py） | 内部调用 playwright 的 `stock_homepage` + `financial_quarterly` 获取财务数据 + 腾讯行情API获取实时价 |

### 2.4 双实现工具（3 个，有 playwright enrichment 版本）

这三个工具默认走 **playwright 版本**（`default_config.py` 的 `tool_vendors` 默认 `"playwright"`），a_stock 直连 HTTP 版作为 fallback 通过 `VENDOR_METHODS` 注册：

| 工具 | a_stock 版数据源 | playwright 版数据源 | 调用优先级 |
|---|---|---|---|
| `get_profit_forecast` | 同花顺 HTTP 直连 | 同花顺F10（playwright） | playwright → a_stock |
| `get_concept_blocks` | 百度股市通 HTTP | 同花顺问财（playwright） | playwright → a_stock |
| `get_fund_flow` | 东财 push2 HTTP | 同花顺问财（playwright） | playwright → a_stock |

> **注意**：`route_to_vendor` 的 fallback 链只对 `AlphaVantageRateLimitError` 生效；playwright 版工具内部把失败包装成错误文本返回（连接失败/熔断中），**不会**自动回退到 a_stock。playwright 服务不可用时，可把对应 `tool_vendors` 改回 `"a_stock"` 手动切换。

---

## 三、7 个 Analyst 按人排列的工具列表

### 3.1 Fundamentals Analyst（基本面分析师）

**文件**: `tradingagents/agents/analysts/fundamentals_analyst.py`

**工具数**: 12

| 工具 | 类别 | 浏览器依赖? | 用途 |
|---|---|---|---|
| `get_fundamentals` | HTTP直连 | ❌ | 实时估值数据（PE/PB/市值/换手率）— 腾讯行情API |
| `get_balance_sheet` | HTTP直连 | ❌ | 资产负债表 — 新浪财经 |
| `get_cashflow` | HTTP直连 | ❌ | 现金流量表 — 新浪财经 |
| `get_income_statement` | HTTP直连 | ❌ | 利润表 — 新浪财经 |
| `get_profit_forecast` | 双实现 | ⚠️ 可选 | 机构一致预期(EPS/评级/研报) — 同花顺F10 playwright(默认)或同花顺HTTP直连 |
| `get_industry_comparison` | HTTP直连 | ❌ | 全行业90个行业横向对比(涨跌幅/成交额/净流入排名) — 东财datacenter |
| `get_financial_quarterly` | playwright | ✅ **是** | 最近4期财务综合数据(营收/净利/ROE/毛利率/负债率等) — 同花顺F10 finance.html |
| `get_stock_homepage` | playwright | ✅ **是** | 综合概要(PE动态/静态、PB、总市值、质押比例、大盘/小盘分类) — 同花顺F10 |
| `get_stock_industry_peers` | playwright | ✅ **是** | 同行业公司财务指标对标(行业排名、同行EPS/ROE/毛利率对比) — 同花顺F10 |
| `get_insider_transactions` | playwright | ✅ **是** | 公司大事(财报/公告/融资融券/大宗交易/业绩预告/质押/增减持等)+高管持股变动+机构调研 — 同花顺F10 event.html（经 playwright_service） |
| `get_stock_holder` | playwright | ✅ **是** | 股东研究(10期股东人数+5期前十大流通股东+退出股东+同业对比) — 同花顺F10 |
| `verify_stock_valuation` | 组合 | ✅ **间接** | 精确Decimal验算PE/PB/ROE/市值 — 底层调用playwright+腾讯行情 |

### 3.2 Market Analyst（技术分析师）

**文件**: `tradingagents/agents/analysts/market_analyst.py`

**工具数**: 7

| 工具 | 类别 | 浏览器依赖? | 用途 |
|---|---|---|---|
| `get_stock_kline_full` | playwright | ✅ **是** | 完整K线数据(含换手率/涨跌幅/成交量, 约120根) — 东财push2his |
| `get_indicators` | HTTP直连 | ❌ | 技术指标(MA/MACD/RSI/布林带等) — mootdx TCP |
| `get_market_context` | playwright | ✅ **是** | 大盘概况(7大指数走势+均线+MACD+换手率+近5日K线+两市成交额+涨跌家数+北向南向资金+融资余额+领涨板块) — 东财页面 |
| `get_stock_levels` | playwright | ✅ **是** | 支撑位/压力位 — 同花顺问财 |
| `get_chip_distribution` | HTTP直连+算法 | ❌ | 筹码分布(获利比例/平均成本/90%集中度/70%集中度) — 东财push2his + Python CYQ算法(150价格桶+换手率衰减) |
| `get_industry_hotmap` | playwright | ✅ **是** | 行业热力(行业涨跌幅/主力净流入热力图) — 东财星图 |
| `analyze_pattern` | 组合 | ✅ **间接** | K线形态识别(12种: 十字星/锤子线/吞没/早晨之星/双底/放量突破/箱体震荡等) — 底层走playwright获取K线 |

### 3.3 News Analyst（新闻分析师）

**文件**: `tradingagents/agents/analysts/news_analyst.py`

**工具数**: 2

| 工具 | 类别 | 浏览器依赖? | 用途 |
|---|---|---|---|
| `get_news` | HTTP直连 | ❌ | 个股新闻 — 东方财富HTTP(主) + 新浪财经(fallback) |
| `get_global_news` | HTTP直连 | ❌ | 宏观新闻(财经快讯) — 财联社 cls.cn |

### 3.4 Policy Analyst（政策分析师）

**文件**: `tradingagents/agents/analysts/policy_analyst.py`

**工具数**: 2

| 工具 | 类别 | 浏览器依赖? | 用途 |
|---|---|---|---|
| `get_news` | HTTP直连 | ❌ | 政策相关个股新闻 — 东方财富 + 新浪财经 |
| `get_global_news` | HTTP直连 | ❌ | 宏观政策新闻 — 财联社 cls.cn |

> **注意**: Policy Analyst 的工具集与 News Analyst **完全相同**，区别在于 Prompt 中要求从政策角度解读数据。

### 3.5 Social Media Analyst（市场情绪分析师）

**文件**: `tradingagents/agents/analysts/social_media_analyst.py`

**工具数**: 4

| 工具 | 类别 | 浏览器依赖? | 用途 |
|---|---|---|---|
| `get_news` | HTTP直连 | ❌ | 舆情新闻，推断市场情绪方向 — 东方财富 + 新浪财经 |
| `get_fund_flow` | 双实现 | ⚠️ 可选 | 个股主力资金流向 + DDE散户数量(反向情绪指标) — 问财playwright(默认)或东财push2 |
| `get_market_context` | playwright | ✅ **是** | 大盘情绪参考(7大指数走势+北向资金+融资余额) — 东财页面 |
| `get_industry_hotmap` | playwright | ✅ **是** | 行业热力情绪参考 — 东财星图 |

### 3.6 Hot Money Tracker（游资追踪分析师）

**文件**: `tradingagents/agents/analysts/hot_money_tracker.py`

**工具数**: 13

| 工具 | 类别 | 浏览器依赖? | 用途 |
|---|---|---|---|
| `get_stock_kline_full` | playwright | ✅ **是** | K线+成交量异动识别(放量/换手率飙升) — 东财push2his |
| `get_news` | HTTP直连 | ❌ | 游资/龙虎榜相关新闻 — 东方财富+新浪 |
| `get_insider_transactions` | playwright | ✅ **是** | 公司大事+内部人交易动向 — 同花顺F10 event.html（经 playwright_service） |
| `get_hot_stocks` | HTTP直连 | ❌ | 当日强势股+题材归因reason tags(同花顺编辑团队人工标注) — 同花顺HTTP |
| `get_northbound_flow` | HTTP直连 | ❌ | 北向资金流向(沪深股通，2024年起实时净买入已停止发布) — 同花顺 hsgtApi + 本地缓存 |
| `get_concept_blocks` | 双实现 | ⚠️ 可选 | 个股所属概念板块列表 — 问财playwright(默认)或百度股市通HTTP |
| `get_fund_flow` | 双实现 | ⚠️ 可选 | 主力资金净流入(超大单+大单) + DDE散户数量 — 问财playwright(默认)或东财push2 |
| `get_dragon_tiger_board` | HTTP直连 | ❌ | 龙虎榜上榜记录+买卖席位明细(营业部)+机构参与情况 — 东财datacenter |
| `get_industry_comparison` | HTTP直连 | ❌ | 行业轮动分析(90个行业涨跌幅/成交额/净流入排名) — 东财datacenter |
| `get_industry_hotmap` | playwright | ✅ **是** | 行业热力(资金轮动方向) — 东财星图 |
| `get_stock_position` | playwright | ✅ **是** | 机构持股明细(哪些机构在买入/卖出、持仓占比变化) — 同花顺F10 |
| `get_stock_holder` | playwright | ✅ **是** | 筹码集中度(股东人数变化+前十大流通股东变动) — 同花顺F10 |
| `get_limit_up_pool` | HTTP直连 | ❌ | 涨停池/连板梯队(连板数/封板资金/炸板次数/所属行业) — 东财push2ex |

### 3.7 Lockup Watcher（解禁监控分析师）

**文件**: `tradingagents/agents/analysts/lockup_watcher.py`

**工具数**: 6

| 工具 | 类别 | 浏览器依赖? | 用途 |
|---|---|---|---|
| `get_insider_transactions` | playwright | ✅ **是** | 公司大事+内部人交易+重要事件 — 同花顺F10 event.html（经 playwright_service） |
| `get_news` | HTTP直连 | ❌ | 解禁/减持相关新闻和公告 — 东方财富+新浪 |
| `get_lockup_expiry` | HTTP直连 | ❌ | 限售解禁日历(历史解禁+未来90天待解禁+解禁数量/占比/影响评估) — 东财datacenter |
| `get_stock_basic` | playwright | ✅ **是** | 股本结构(总股本/流通股本/限售股/多期历史变化) — 同花顺F10 |
| `get_stock_holder` | playwright | ✅ **是** | 股东研究(股东人数+前十大流通股东+退出股东) — 同花顺F10 |
| `get_stock_equity_history` | playwright | ✅ **是** | 股本历史变动(回购/送转/增发及原因) — 同花顺F10 |

---

## 四、浏览器依赖汇总

| 分析师 | 总工具数 | 纯playwright工具 | 间接依赖(组合) | 双实现(可选) | 零浏览器依赖(纯HTTP) |
|---|---|---|---|---|---|
| **Fundamentals** | 12 | 5 | 1 | 1 | 5 |
| **Market** | 7 | 4 | 1 | 0 | 2 |
| **News** | 2 | 0 | 0 | 0 | 2 |
| **Policy** | 2 | 0 | 0 | 0 | 2 |
| **Social Media** | 4 | 2 | 0 | 1 | 1 |
| **Hot Money** | 13 | 5 | 0 | 2 | 6 |
| **Lockup** | 6 | 4 | 0 | 0 | 2 |

**全局统计**（去重后的唯一个体工具）:

| 类别 | 数量 |
|---|---|
| 纯playwright工具 | 12 |
| 间接依赖(组合工具，底层走playwright) | 2 |
| 双实现工具(有playwright enrichment版本) | 3 |
| 纯HTTP直连(零浏览器依赖) | 16 |
| **合计唯一个体函数** | **33** |

> **关键发现**: 虽然剧本中注册了 12 个 playwright 工具和 2 个间接工具，但 Hot Money Tracker（13个工具，最强工具集）的 13 个工具中有半数不需要浏览器。纯浏览器依赖的工具主要集中在**基本面数据获取**（同花顺F10的财务/股东/股本/持仓/大事页面）、**行情数据**（东财K线/大盘/支撑压力）和**行业热力**（东财星图）。

---

## 五、工具注册架构

所有工具的注册路径：

```
@tool 定义（xx_tools.py）
    → agent_utils.py（统一导入）
        → analyst 文件（tools 列表）
            → trading_graph.py（ToolNode(name, tools) 注册，7个独立的 ToolNode）
```

7 个 ToolNode 与 7 个 Analyst 一一对应：

| ToolNode 名称 | 对应 Analyst |
|---|---|
| `market` | Market Analyst |
| `social` | Social Media Analyst |
| `news` | News Analyst |
| `fundamentals` | Fundamentals Analyst |
| `policy` | Policy Analyst |
| `hot_money` | Hot Money Tracker |
| `lockup` | Lockup Watcher |

数据路由通过 `interface.py` 的 `VENDOR_METHODS` 实现（19 个方法），vendor 优先级链：`a_stock` > `playwright` > `yfinance` > `alpha_vantage`。

> **playwright vendor 延迟注册**：`interface.py` 模块加载末尾调用 `_register_playwright_vendor()`（惰性 import `playwright_tools.py`，避免循环导入），把 `get_profit_forecast` / `get_concept_blocks` / `get_fund_flow` 三个方法的 playwright 实现注册进 `VENDOR_METHODS`。
>
> **双实现工具默认优先 playwright**：`default_config.py` 的 `tool_vendors` 把上述三个方法默认设为 `"playwright"`（问财/F10 数据更丰富）。playwright 服务不可用时工具返回错误文本而非异常，`route_to_vendor` 的 fallback 链（仅对 `AlphaVantageRateLimitError` 触发）不会自动接管，需手动把 `tool_vendors` 改回 `"a_stock"`。

> **限流保护**: 所有指向 `eastmoney.com` 的 HTTP 请求统一走 `_em_get()` 节流入口：`threading.Lock` + 模块级时间戳串行限流（默认间隔 1.0s）+ 随机抖动(0.1~0.5s) + 复用 `requests.Session`(Keep-Alive)。
